{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX|Linux) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/mango-panel",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}
